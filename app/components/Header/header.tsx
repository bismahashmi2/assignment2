import React from "react";
import styles from "./header.module.css";

const Header = () => {
  return (
    <div>
      <header className={styles.head}>This is header</header>
    </div>
  );
};

export default Header;
